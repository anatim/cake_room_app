<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nastisch.cakeroom">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".AndroidLoginController"
        android:allowBackup="true"
        android:icon="@mipmap/app_logo_cake_room_72"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo_cake_room_72"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartScreenAc">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ShoppingCartActivity" />
        <activity android:name=".CheckoutAc" />
        <activity android:name=".DrawerAc" />
        <activity android:name=".GalleryAc" />
        <activity android:name=".ProductDetailsActivity" />
        <activity android:name=".ShoppingCartEntry" />
        <activity android:name=".LoginAsStaffAc" />
        <activity android:name=".StaffAccountAc" />
        <activity android:name=".FragManageProducts" />
        <activity android:name=".FragManageContractors" />
        <activity android:name=".FragManageLoyalty" />
        <activity android:name=".AboutUsAc" />
        <activity android:name=".ContactUsAc" />
        <activity android:name=".RegisterAc" />
        <activity android:name=".LoyaltyAc" />
        <activity android:name=".FragAboutUs" />
        <activity android:name=".FragContactUs" />
        <activity android:name=".FragLoyalty" />
        <activity android:name=".FragStores" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".OurStoresAc"
            android:label="@string/app_name" />
        <activity android:name=".LoginAc" />
        <activity android:name=".UserAccountAc" />
    </application>

</manifest>